/*
#include <stdio.h>

#define NUM_ESAMI 3
#define NUM_STUDENTI 150

#define PR1 0
#define FDI 1
#define AM 2

int matrixes() {
	int pr1[NUM_STUDENTI];

	int esami[NUM_ESAMI][NUM_STUDENTI]; // Matrice con NUM_ESAMI righe e NUM_STUDENTI colonne

	esami[FDI][3]; // Accedo al voto dello studente 3 in FDI

	// In realtà in memoria questo è salvato come una unica riga, ad esempio esami[PR1][151], sto accedendo a 2 di FDI

	// Init Matrix
	for (int r = 0; r < RIGHE; ++r) { // Un for per loopare nelle righe

		for (int c = 0; c < COLONNE; ++c) { // Per ogni riga loopo per ogni colonna
			matrix[r][c] = a;
		}
	}

	// Init Matrix

	esami={{1,2,3}, {1,2,3}, {1,2,3}};
	return 0;
}
*/
