/* Scrivere un programma che permetta di convertire numeri binari in decimale. Il
 * programma acquisisce prima il numero di cifre che formano il numero binario,
 * dopodiché acquisisce una per volta le cifre del numero binario. Il numero binario deve
 * essere memorizzato in un array. Supporre che il numero binario possa avere al
 * massimo 20 cifre.
 */

int main(){



	return 0;
}